import angular from 'angular';

import CompleteReservation from './complete-reservation.component';

export default angular.module('component.complete-reservation', [])
  .component('completeReservation', CompleteReservation)
  .name;
